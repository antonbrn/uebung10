package h1;

public class H1_main {

	public static void main(String[] args) {

	
	}

	/**private static void printNextGen(int n) {
		feld.computeGeneration(n);
		for (int i = 0; i < feld.getGridArray().length; i++) {
			for (int j = 0; j < feld.getGridArray()[0].length; j++) {
				String zeichen;
				if (feld.getGridArray()[i][j].isAlive()) {
					zeichen = "Ø";
				} else {
					zeichen = "•";
				}
				System.out.print("" + zeichen + " ");
			}
			System.out.print("\n");
		}
		System.out.print("\n");
	}**/

	// Cell[] leben = {new Cell(0,0), new Cell(1,1), new Cell(2,2), new Cell(3,3),
	// new Cell(4,4), new Cell(5,5)};
	// Grid feld = new Grid(leben, 5, 5);


}
